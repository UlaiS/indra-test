<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.ulai.indra">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="mx.ulai.indra.application.IndraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:vmSafeMode="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.Indra">

        <meta-data
            android:name="mx.ulai.indra.application.componentOverride"
            android:value="${indra_application_component}" />

        <activity android:name="mx.ulai.indra.ui.activity.navigation.NavigationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>